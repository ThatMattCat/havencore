# vllm[audio] intentionally unpinned — the base image ships a dev build
# (0.10.1.dev1+gbcc0a3cbe) that pypi cannot resolve. The base image digest
# is the effective vllm pin; the [audio] extra only adds optional deps.
vllm[audio]
gradio==6.12.0
requests==2.32.4
